import { makeStyles } from "@material-ui/core"

const useDropContainerStyles = makeStyles({
  root: {
    border: "1px solid black",
    padding: "2px",
    height: "100%",
    width: "150px",
  },
})

export default useDropContainerStyles
